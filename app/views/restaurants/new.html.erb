<!DOCTYPE>
<html>

<head>

</head>

<body>
	 <%= form_for @restaurant do |f| %>
	 <%= f.label :name %>
	 <%= f.text_field :name, placeholder: 'Add restaurant name' %>
	 <p></p>
	 <%= f.label :description %>
	 <%= f.text_field :description, placeholder: 'Add a restaurant description' %>
	  <p></p>
	  <%= f.submit "Create Restaurant" %>
	 <% end %>

	 <% if flash[:error] -%>
		 <%= flash[:error] %>
	 <% end -%>
</body>

</html>

